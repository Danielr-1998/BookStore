// Definimos una interfaz para las propiedades del componente Dashboard
export interface DashboardProps {
    pendientes: number;
    atendidas: number;
    canceladas: number;
    reprogramadas: number;
}
